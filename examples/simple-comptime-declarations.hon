# comptime constants
DEBUG :: true
PI: f64 :: 3.14
ANSWER :: 42
BAD_INDEX :: -1
PI_SQUARED :: PI * PI
PI_CUBED :: PI * PI * PI
SHOULD_WORK :: ANSWER * PI  # ANSWER should be promoted to f64 in this case
X: f32 :: 10  # should be accepted as an f32 (even though it's declared without decimals - becomes 10.0 f32)
