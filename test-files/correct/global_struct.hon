Point :: c struct {
    x: i32,
    y: i32,
}

ORIGIN :: Point{ .x = 0, .y = 0 }

p := Point{ .x = 42, .y = 54 }

main :: func() i32 {
    mut result := p.x + p.y
    result = result + ORIGIN.x
    return result
}
